kind = "web"
previewPath = "/"
title = "LoanLedger"
version = "1.0.0"
id = "artifacts/loan-ledger"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20337

[services.development]
run = "pnpm --filter @workspace/loan-ledger run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/loan-ledger", "run", "build" ]
publicDir = "artifacts/loan-ledger/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20337"
BASE_PATH = "/"
